services:
  - type: web
    name: cloudshield-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "dashboard.app:create_app()" -b 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
